/**
 * ============================================================
 * 웰컴 체크인 — 공통 설정 (checkin_data.js) — v17
 * ============================================================
 * v17부터 CHECKIN_PLAN(문항 자체 — 발문·선택지·사료)은 이 파일이 아니라
 * Google Sheets("체크인문항" 탭)에서 관리합니다. 교사 대시보드 "체크인 > 문항 관리"
 * 에서 추가·수정·삭제하면 됩니다 (또는 시트를 직접 편집).
 * 이 파일은 페이지 로딩 시점에 mode=checkinPlan API로 CHECKIN_PLAN을 채우는
 * loadCheckinPlan()과 짝을 이루고, 여기 남은 CHECKIN_CONFIG(온도 척도·기본 단어칩·
 * 기타입력 설정)만 계속 정적으로 관리합니다.
 * ============================================================
 */

const CHECKIN_CONFIG = {

  /* ── 공통: 감정 온도 (모든 차시에서 수집) ── */
  temperature: {
    enabled: true,
    scale: 5,
    prompt: "지금 내 상태는?",
    labels: {
      1: "완전 방전",
      2: "좀 처짐",
      3: "보통",
      4: "괜찮음",
      5: "풀충전"
    }
  },

  /* ── 공통: 기본 단어칩 (차시별 override 없을 때 사용) ──
   * 에너지 축(고/저) × 유쾌함 축(긍/부) 기준으로 배치.
   * 고·긍: 기대, 설렘 / 고·부: 짜증, 긴장 / 저·긍: 편안 / 저·부: 피곤 */
  defaultWordChips: ["기대", "설렘", "편안", "짜증", "긴장", "피곤"],

  /* ── 공통: 기타 직접입력 ── */
  customInput: {
    enabled: true,
    maxLength: 10,
    placeholder: "직접 쓰기",
    notice: "선생님만 볼 수 있어"   // 익명 아님을 처음부터 명시
  }
};
