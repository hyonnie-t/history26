# hyonnie.md

CLAUDE.md는 이 레포 코드 구조 설명(자동 생성, 건드리지 않음).
이 파일은 코드에는 안 드러나는 맥락 — 대화 원칙, 반복 실수, 외부 도구 규칙을 담는다.
인프라 정보(URL/ID/토큰 등 민감정보)는 `효니.local.md`에 별도 보관 — 이 레포는 public이므로
효니.local.md는 반드시 `.gitignore`에 포함시킬 것. 커밋 전 diff에 민감정보 없는지 항상 확인.
**백엔드 파일(`*.gs`)은 어떠한 경우에도 이 레포(GitHub)에 올리지 않는다 — 필요하면 클로드 코드
작업창(채팅) 내에서만 주고받는다.** (과거 `backend_v24.gs`가 실수로 커밋됐다 삭제된 사고 있음,
5번 섹션 참조 — 파일을 지워도 git 히스토리에는 남으므로 재발 방지가 핵심.)

코드 작업 전에도, 콘텐츠(활동지·수업설계) 작업 전에도 먼저 읽을 것.

---

## 0. 대화·피드백 원칙

- 반말, 호칭은 "효니"
- 업무·사고 정리는 결론 먼저, 이유는 뒤. 감정/개인 얘기는 수용 먼저
- 틀린 건 "틀렸어, 이유는 이거야" 방식으로 명확히 — 애매하게 감싸지 않기
- 모르는 건 모른다고 말할 것. 그럴듯하게 채우지 않기
- 확실한 것만 단언, 논쟁적인 건 "논쟁 있어" 명시
- 긴 코드는 파일로, 채팅에 바로 출력 금지
- 전면 재작성보다 타겟 수정(str_replace 등) 선호, 수정 후 검증

---

## 1. 배경

- 효니: 중학교 역사 교사. 2학년 세계사, 3학년 한국사 담당 (담당 반은 효니.local.md 참조)
- 동료 교사와 같은 포털 공유, 담당 반 분리 운영 중 (세부는 효니.local.md 참조)

---

## 2. 기술 규칙 — 코드 작업 전 항상 확인

### 재배포
Apps Script는 반드시 **"배포 관리 > 수정 > 새 버전"**으로 갱신.
"새 배포"로 만들면 URL이 바뀌어서 프론트 전체가 깨짐.

### 저장소 분리 규칙
- 이 레포(`history26`)는 **프론트엔드 전용**. `.gs` 백엔드 파일은 절대 커밋하지 않는다.
  실수로 커밋됐다면 즉시 삭제 커밋으로 끝내지 말고, 파일에 담겼던 토큰/키는 노출된 것으로
  간주하고 교체까지 해야 한다(git 히스토리에는 계속 남기 때문).
- 백엔드 코드를 클로드 코드와 함께 봐야 할 때는 파일을 레포에 넣지 말고 채팅(작업창)에
  붙여넣거나 업로드해서 그 세션 안에서만 주고받는다.

### 반복된 실수 3가지 (매번 점검)
1. `CONFIG.SHEET_WEBAPP_URL` 등 긴 URL/ID를 옮겨 적을 때 오탈자 검증(원본과 diff) 없이 넘기지 말 것
   — 조용히 전송 실패하는 원인.
2. 초기화 코드를 IIFE로 즉시 실행하면서 파일 아래쪽에 선언된 const(DOM 참조 등)를 참조하지 말 것
   — TDZ 에러로 스크립트 전체가 멈추고 이후 모든 버튼/입력이 죽음.
   init은 함수로 선언, 모든 const·이벤트 바인딩 끝난 뒤 스크립트 맨 아래에서 호출.
3. 카드 나열형 활동(순서 재배열 등)은 탭-스왑 하나로 때우지 말고,
   드래그(SortableJS, handle 지정) + 화살표 버튼(▲▼) 둘 다 기본 제공
   — 태블릿에서 드래그 안 먹는 경우 대비.

### Gemini API
- 키 형식: 구버전 `AIza`(35~45자) → 신형 `AQ.`(~53자, 2026.06~). 유효성 검사 로직 신형 인식 확인.
- 배치 처리: 8개씩 + 15초 간격 (RPM 5 제한). 429 오류 시 Flash-Lite로 자동 폴백.

### 패들렛
- 데이터 추출은 **Excel 내보내기(CSV 아님)** — 본문 텍스트 포함되는 유일한 방법.
- API 접근: `wall_hashid` 추출 후 `/api/10/wishes` 직접 호출.
- 반별 패들렛 URL 재사용 시 효니.local.md 참조.

### 개별 웹앱 신규 제작 시 표준 규칙
- `CONFIG.GAME_NAME`은 config.js의 `id`와 정확히 일치
- URL 파라미터 `?sid=학번&name=이름` 자동 채움 + 미리보기(`?preview=1`, 제출해도 시트 미반영) 기본 포함
- 교사용 대시보드/토큰은 개별 웹앱에 넣지 않음 — 포털이 백엔드(teacherGet_) 직접 호출하므로 중복이고,
  학생 페이지 소스에 토큰 노출되는 문제 있음
- 학번 형식: 5자리 (학년1+반2+번호2, 예: 30506)
- 태블릿 터치 지원, 최소 폰트 16px
- 서술형 입력칸 있는 활동은 "💡 막막하면 힌트 보기" 토글 기본 포함
  (답 대신 맥락 단서만 — 비교 대상 제시, 관점 전환 질문 등)
- **색감·톤은 매번 지시받지 않고 주제·시대에 맞게 알아서 조합한다** (효니 확인, 2026-09-03).
  "예쁘게" 정도의 요청에도 임의로 색상표를 새로 짜되, 왜 그 색을 골랐는지 코드 주석 한 줄로 남길 것 —
  같은 시대를 다루는 다른 차시와 겹치는 배색을 그대로 복붙하지 않는지 스스로 점검.
- **디자인 큰 틀은 "역사책 페이지" 컨셉이 기준선이다** (`crusades` 레포, 2026-09-03 리디자인 이후).
  화려한 모바일 앱 느낌(굵은 라운드 버튼, 진한 드롭섀도, 알약형 배지, 채도 높은 원색)을 기본값으로
  쓰지 말 것. 대신: 본문은 세리프(Noto Serif KR), 라벨·버튼 같은 UI 조작 요소만 산세리프로 분리 /
  카드는 얇은 괘선 + 작은 radius(4~6px) + 옅은 그림자 / 강조는 채도 낮은 인장색·고금색 한두 개로 절제 /
  선택 상태는 꽉 찬 색 채우기 대신 옅은 워시+체크 표시 같은 절제된 신호로. 주제가 확 다르면(예: 현대사,
  통계형 활동) 이 톤에서 벗어나도 되지만 그 경우에도 "채도 높은 앱 UI"로 기본 회귀하지 않도록 의식할 것.

### 한국 에듀테크 도구 관련
뤼튼·클래스팅·네이버 웨일 등 기능은 업데이트가 빠르니 직접 확인 전엔 "있을 수 있는데 확인해봐"로만 말할 것.
확정처럼 단언 금지.

---

## 3. 수업 설계 철학 (콘텐츠 작업 시)

핵심: 설명 줄이고 해석하게 만들되, 사실은 정확하게.

- 절대 원칙: 확인된 사실만 단언, 불확실하면 "확인 필요"라고 먼저 말함. 논쟁적 해석은 명시. 그럴듯한 내용으로 빈자리 채우기 금지
- 구조: ①질문으로 시작(바로 설명 금지) → ②탐구 활동(학생이 해석) → ③개념 정리(활동 이후) → ④확장 질문(현재/타시대 연결)
- 활동 3요소 필수: 학생 행동(쓰기/토의/선택/판단) + 사고 요구 질문 + 말로 표현하는 단계
- Scaffold: 수업 중엔 예시 제공 가능하되 학생이 먼저 써본 뒤 공개 (예시=비교 대상, 정답 아님). 수행평가는 scaffold 낮춤
- 글쓰기: 줄거리 요약/느낌 쓰기 금지 → 관점 쓰기, 선택 분석, 현재 연결 선호. 중2 눈높이 구어체
- Padlet 글쓰기: 정답 있는 구조는 AI 대리작성 쉬워지므로 차단 — 역할 기반 관점, 선택+이유 구조로 설계
- 역할극/창작: 사료·교과서 근거 연결 또는 "창작 자료"임을 명시. 창작→분석→직접제작 순서 기본
  (단, 사료가 교과서에 직접 제시된 경우 사료독해→압축재표현도 유효)
- 게임: 학습 피드백 메커니즘(결과→글쓰기·토의 연결) 필수, 역사적으로 모호한 내용은 구현 전 제거,
  힌트는 맥락 단서만
- 내용 구성: 비교/해석/연결 중 최소 1개 포함
- 출력은 항상 A(안정형)/B(탐구형)/C(참여형) 2~3안 제시, 각 안에 목표·핵심질문·활동흐름·학생활동예시·교사개입포인트

### 확립된 역사 해석 (재확인 불필요)
- 카노사 굴욕 핵심: 파문=종교적 죽음→사회적 매장→봉신 충성서약의 신학적 근거 소멸. 단순 권력다툼 프레임 금지
- 교황권 궤적: 상승→하락 단일 호선. "카노사→아비뇽 반전" 아님

### 클리포(Clipo) 루브릭 원칙
- 구간별 독립 판단, 절대 불가 조건은 전 구간 반복 명시
- AI 임의 기준(논리성·풍부함·명확성 등) 차단 문구 포함
- 구간당 예시 2개 이상, 500자 제한
- A+(최상)는 2단계 확인: 표면체크(다인자연결·반론고려·현재연결 중 1개) + 교육과정 근거 확인
  ("적절하고 타당한 근거를 가지고 역사적 사실에 대해 해석, 판단하기") — A+는 배포용 텍스트에 미포함
- 중학생 수준 허용적 기준, 감점 기반 구조 선호

### 세특 작성
- 세특 작성지침·에이전트 프롬프트 별도 운영 중 (프로젝트 파일 참조)
- 학생 글 직접 인용 유지, 이중 평가 태그 금지, 과정·기능 태그 1세특당 최대 1회

---

## 4. 현재 상태 / 진행중 (⚠️ 학교 특정 정보 없는 항목만. 반/과목 세부는 효니.local.md)

> 2026-08-29 클로드 코드로 정합성 확인 완료. 결과는 아래 체크리스트 참조.

- [x] 활동목록JSON·순차진행 완료 여부 확인
  → **완료 구현돼 있음.** `index.html`의 `l.activities` 배열 렌더링(`renderLessonCard`
  `index.html:1113`, `renderActivityBlock` `index.html:1161`), `l.sequential`로 다음 활동
  잠금(`locked` 계산, `index.html:1125`), 차시 완료 판정 AND 조건(`lessonDone()` `index.html:955`,
  대시보드 쪽 `lessonAllActivityIds_()` `index.html:2740`)까지 모두 존재.
- [x] 칭호첩 배지 시스템 — 판정 로직(`computeBadges_`) 소재 확인
  → 이 레포엔 없음(정적 프론트라 `.gs` 파일 자체가 없음). 프론트는 `renderBadges`/
  `checkNewBadges`(`index.html:1430`, `1459`)로 **렌더링만** 담당하고, 실제 배지 획득 판정은
  외부 `backend_v23.gs`의 `computeBadges_`가 `mode=student` 응답의 `STUDENT_DATA.badges`로
  내려주는 값을 그대로 그린다 (`index.html:96, 1406~1407` 주석에 명시).
  ⚠️ **2026-09-03 정정**: "`backend_v23.gs`" 표기는 옛 파일명 그대로 남은 것 — 실제로는
  `hyonnie-t/history26_backend` 레포의 `code.gs`가 지금의 `computeBadges_`다(같은 세션에서
  직접 열어서 확인). 클로드 코드가 이걸 헷갈려서 "저장소 어디에도 없는 외부 파일"이라고
  잘못 말한 적 있음 — 배지 관련 작업 전에 반드시 `history26_backend`부터 열어볼 것,
  "backend_v23.gs"라는 이름을 실제 파일로 착각하지 말 것.
- [x] `ai_module_*.gs`, `backend_*.gs` 실제 최신 버전 확인
  → **이 레포로는 확인 불가.** 프론트 코드 주석에 언급되는 버전은 `backend_v23.gs`,
  `ai_module_v9.gs`뿐이고(`index.html:96,1406`, `2925`), 이게 "현재 프론트가 전제하는 버전"일 뿐
  실제 Apps Script 프로젝트의 최신 배포본과 같은지는 Apps Script 콘솔에서 직접 봐야 함.
  ⚠️ 2026-08-29에 `backend_v24.gs`가 실수로 이 레포(main)에 커밋됐다가 삭제된 사고 발생 —
  삭제해도 git 히스토리엔 남으므로 그 파일에 있던 교사 토큰(`his26`, `mh26`)은 노출된 것으로
  보고 교체 필요. 이후로는 `.gs` 파일은 레포에 절대 올리지 않고 클로드 코드 작업창에서만
  주고받기로 함(위 "저장소 분리 규칙" 참조).
- [x] `BAN_SIZE` 반별 실제 값 확인
  → `config.js:31~34`. 2학년 1~4반(27/26/27/27), 5~8반 0(미담당) / 3학년 5~8반(26/27/26/26),
  1~4반 항목 없음(미담당) — 효니.local.md의 담당 반 설명과 일치 확인.

- [x] 3차시 "역사법정: 권문세족의 성장" 개별 웹앱 (`hyonnie-t/his_judge_goryeo`) 신규 제작
  (2026-09-03) → 검사팀/변호인팀/배심원 역할극. 판사 역할·전자투표·내장 타이머는 설계
  중 제외 결정. `history26_backend`의 기본 gameName 제출 경로만 쓰고 새 action은 안 씀.
  이 신규 활동에 맞춰 `history26_backend/code.gs`에 두 가지를 새로 추가함(둘 다
  **2026-09-03 기준 배포 대기 — Apps Script에 아직 반영 안 됨**):
  1. `courtCase` 조회 action — 역사법정 제출(검사/변호인 발언, 배심원 판결)을 완전
     별도 시스템인 `hyonnie-t/re_judge`(학생 갈등조정 도구)의 사건 접수 형식으로
     취합해줌. re_judge 쪽 코드는 안 건드림 — 반자동(교사가 결과 복사해서 넣는
     방식)으로 결정, 완전 자동 연동(두 시스템 간 토큰 공유 필요)은 보류.
  2. 역할극 칭호 — `computeBadges_`에 판정 추가, "논고왕"⚖️(검사)/"변론왕"🛡️(변호인).
     `choicesJson.role` 필드로 판정해서 gameName 특정 안 함 — 나중에 다른 역할극
     웹앱을 만들어도 role만 같은 값으로 채우면 자동 적용됨.

세부 수업 설계 진행상황(주제별, 단원별)은 효니.local.md 참조.

---

## 5. 도구 목록

- Gemini API (AI Studio, 무료 티어) — 학생 서술 분석
- 클리포(Clipo) — 루브릭 기반 AI 채점
- sengpt — 세특 작성 에이전트
